import React from "react";

import "./StartScreen-Title.scss";

const StartScreenTitle: React.FC = ({ children }) => {
  return <div className="StartScreen-Title">{children}</div>;
};

export default StartScreenTitle;
