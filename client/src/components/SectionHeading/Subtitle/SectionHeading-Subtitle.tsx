import React from "react";

import "./SectionHeading-Subtitle.scss";

const SectionHeadingSubtitle: React.FC = ({ children }) => {
  return <div className="SectionHeading-Subtitle">{children}</div>;
};

export default SectionHeadingSubtitle;
